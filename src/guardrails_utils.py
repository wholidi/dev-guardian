# guardrails-ai is incompatible with openai v1+ — disabled for demo mode

from typing import Any, List, Dict

def guard_findings(findings: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
    return findings
